# 서브차트 기본값. root values.yaml 의 db: 블록이 덮어쓴다.
image:
  repository: library/postgres
  tag: "16"
auth:
  username: app
  password: changeme   # 예시용. 실제로는 외부 Secret 참조 권장
  database: appdb
persistence:
  size: 5Gi
service:
  port: 5432
