# 서브차트 기본값. root values.yaml 의 jenkins: 블록이 덮어쓴다.
image:
  repository: jenkins/jenkins
  tag: lts-jdk21
persistence:
  size: 10Gi
service:
  port: 8080
ingress:
  enabled: false
  className: nginx
  host: jenkins.local
  path: /
  annotations: {}
